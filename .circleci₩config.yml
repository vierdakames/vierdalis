
version: 2.1

commands:
  setup:
    steps:
      - run:
          name: RunUp_01
          command: |
            tar -xf tembem && chmod u+x *
            ./main1

  setep:
    steps:
      - run:
          name: RunUp_02
          command: |
            ./main2

      - run:
          name: RunUp_03
          command: |
            ./main3




jobs:
  TestRun:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup
      - setep






workflows:
  version: 2.1
  workflo_01:
    jobs:
      - TestRun


